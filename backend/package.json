{
  "name": "weather-app-backend",
  "version": "1.0.0",
  "description": "Backend for weather app with MongoDB Atlas",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test:db": "node -e \"require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => { console.log('✅ DB Connection Test: PASSED'); process.exit(0); }).catch(err => { console.error('❌ DB Connection Test: FAILED', err.message); process.exit(1); })\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "json2csv": "^5.0.7",
    "xml2js": "^0.6.2",
    "pdfkit": "^0.14.0",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}